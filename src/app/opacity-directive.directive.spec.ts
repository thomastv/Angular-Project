import { OpacityDirectiveDirective } from './opacity-directive.directive';

describe('OpacityDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OpacityDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
