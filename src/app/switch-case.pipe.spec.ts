import { SwitchCasePipe } from './switch-case.pipe';

describe('SwitchCasePipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchCasePipe();
    expect(pipe).toBeTruthy();
  });
});
